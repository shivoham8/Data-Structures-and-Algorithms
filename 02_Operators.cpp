#include <iostream>
using namespace std;

int main() {
    int a = 11;
    double b = 5.56;

    // Result will be of bigger data type
    cout << "(a/b) = " << a/b << endl;

    cout << (5/(double)2) << endl;

    // Relational Operators (>, <, <=, >=, ==, !=) returns only true(1) or false(0)

    // Logical Operators --> OR(||), AND(&&), NOT(!)

    // BitWise Operators

    return 0;
}
